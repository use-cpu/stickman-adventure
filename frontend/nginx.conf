worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    sendfile on;
    keepalive_timeout 65;

    # 允许 nginx 在运行时动态解析 K8s Service DNS
    # 不用 resolver 指令的话,nginx 只在启动时解析一次 DNS
    # 如果后端 Service 还没起来或 DNS 还没就绪,nginx 会直接崩
    resolver kube-dns.kube-system.svc.cluster.local valid=30s ipv6=off;
    resolver_timeout 5s;

    server {
        listen 80;
        server_name _;
        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        # Sealos 创建的 Service 带随机后缀,必须用完整 K8s DNS 域名
        # 格式: {service-name}.{namespace}.svc.cluster.local:{port}
        set $backend_upstream "stickman-backend-kuvmvulsvkhj.ns-lbkg4m2d.svc.cluster.local:8080";
        location /api/ {
            proxy_pass http://$backend_upstream/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            # 超时设置
            proxy_connect_timeout 10s;
            proxy_read_timeout 60s;
        }
    }
}
